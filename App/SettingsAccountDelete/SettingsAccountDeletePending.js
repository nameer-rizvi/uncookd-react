import React from "react";
import { GrayBox, P, SVG } from "../Element";

const SettingsAccountDeletePending = () => (
  <GrayBox $row>
    <P>Guess this is goodbye then...</P>
    <SVG name="skull" style={{ marginLeft: 10 }} />
  </GrayBox>
);

export default SettingsAccountDeletePending;
